apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolov5-detection
  labels:
    app: yolov5-detection
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yolov5-detection
  template:
    metadata:
      labels:
        app: yolov5-detection
    spec:
      containers:
        - name: yolov5-detection
          image: djjoel/yolov5-detection:latest
          ports:
            - containerPort: 8501
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1500Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 120
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 8501
            initialDelaySeconds: 120
            periodSeconds: 10
